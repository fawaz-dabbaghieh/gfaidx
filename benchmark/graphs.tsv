# Fill one row per graph. GFA paths may be absolute or relative to data_root.
# graph IDs should be file-safe: letters, digits, dots, underscores, and dashes.
#
# gfaidx_path_names_file is optional. If present, it should be produced by:
#   gfaidx get_path <indexed-graph.gfa.gz> --print_path_names
# and filtered to the P/W records to coordinate-index.
#
# gfaidx_reference is optional and is used only when gfaidx_path_names_file is
# empty. If both are empty and all loci for a graph use one sample, the pipeline
# selects that sample automatically.
#
# odgi_path_indexes defaults to true. Set it to 0/false for graphs with no P/W
# paths, because odgi pathindex can abort on pathless graphs.
graph	gfa	gfaidx_path_names_file	gfaidx_reference	odgi_path_indexes	gfaidx_index_extra	gfaidx_coord_extra	vg_convert_extra	vg_find_extra	vg_chunk_extra	odgi_build_extra	odgi_pathindex_extra	odgi_stepindex_extra	gbz_gbwt_extra
hprc_v2_mc_chr1	hprc_v2_mc_chr1.gfa	""	CHM13	1	""	""	--ref-sample CHM13		""	""	""	""	""
